name: Agent delete repair

on:
  push:
    branches: [fix/restore-agent-delete]

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up pnpm
        run: npm install --global pnpm@11.19.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Restore agent deletion end to end
        shell: bash
        run: |
          set -euo pipefail
          python <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              target = Path(path)
              text = target.read_text()
              if old not in text:
                  raise SystemExit(f'missing patch anchor in {path}: {old[:120]!r}')
              target.write_text(text.replace(old, new, 1))

          replace_once(
              'src/dashboard/api.ts',
              "  AGENT_CREATE_FAILED: '创建客服失败，请重新提交',\n",
              "  AGENT_CREATE_FAILED: '创建客服失败，请重新提交',\n  AGENT_DELETE_FAILED: '删除客服失败，请稍后重试',\n",
          )

          replace_once(
              'src/dashboard/api.ts',
              "export async function getAgentQuotaLedger(\n  id: string,\n): Promise<AgentQuotaLedgerPayload> {\n",
              "export async function deleteAgent(\n  id: string,\n): Promise<{ reassignedConversationCount: number }> {\n  return request(`/api/admin/agents/${encodeURIComponent(id)}`, {\n    method: 'DELETE',\n  });\n}\n\nexport async function getAgentQuotaLedger(\n  id: string,\n): Promise<AgentQuotaLedgerPayload> {\n",
          )

          replace_once(
              'src/dashboard/AdminPortal.tsx',
              "  createAgent,\n",
              "  createAgent,\n  deleteAgent,\n",
          )

          replace_once(
              'src/dashboard/AdminPortal.tsx',
              "  const [saving, setSaving] = useState(false);\n",
              "  const [saving, setSaving] = useState(false);\n  const [deletingAgentId, setDeletingAgentId] = useState<string | null>(null);\n",
          )

          delete_handler = r'''
  async function removeAgent(agent: AgentAccount) {
    if (deletingAgentId) return;
    const confirmed = window.confirm(
      `确定永久删除客服「${agent.name}」？\n\n删除后该账号将立即无法登录，当前未结束会话会释放并重新分配；历史聊天与统计记录会保留。`,
    );
    if (!confirmed) return;

    setDeletingAgentId(agent.id);
    setError('');
    try {
      await deleteAgent(agent.id);
      if (statisticsAgent?.id === agent.id) setStatisticsAgent(null);
      if (draft.id === agent.id) {
        setEditorOpen(false);
        setDraft(emptyAgentDraft);
        resetQuotaLedgerState();
      }
      await refresh();
    } catch (reason) {
      setError(message(reason, '删除客服失败'));
    } finally {
      setDeletingAgentId(null);
    }
  }

'''
          replace_once(
              'src/dashboard/AdminPortal.tsx',
              "  const sectionTitle = section === 'agents' ? '客服坐席' : '流量统计';\n",
              delete_handler + "  const sectionTitle = section === 'agents' ? '客服坐席' : '流量统计';\n",
          )

          replace_once(
              'src/dashboard/AdminPortal.tsx',
              "                                <button\n                                  type=\"button\"\n                                  className=\"table-action\"\n                                  onClick={() => editAgent(agent)}\n                                >\n                                  编辑\n                                </button>\n",
              "                                <button\n                                  type=\"button\"\n                                  className=\"table-action\"\n                                  onClick={() => editAgent(agent)}\n                                >\n                                  编辑\n                                </button>\n                                <button\n                                  type=\"button\"\n                                  className=\"table-action danger-action\"\n                                  aria-label={`删除客服 ${agent.name}`}\n                                  disabled={deletingAgentId === agent.id}\n                                  onClick={() => void removeAgent(agent)}\n                                >\n                                  {deletingAgentId === agent.id ? '删除中…' : '删除'}\n                                </button>\n",
          )

          replace_once(
              'src/dashboard/admin-agents.css',
              "    padding: 0 120px 12px 0;\n",
              "    padding: 0 190px 12px 0;\n",
          )
          replace_once(
              'src/dashboard/admin-agents.css',
              "  min-width: 102px;\n",
              "  min-width: 166px;\n",
          )
          replace_once(
              'src/dashboard/admin-agents.css',
              ".admin-agent-actions .statistics-action:hover {\n  border-color: #bbb7ee;\n  background: #eceaff;\n}\n",
              ".admin-agent-actions .statistics-action:hover {\n  border-color: #bbb7ee;\n  background: #eceaff;\n}\n\n.admin-agent-actions .danger-action {\n  border-color: #f0d0d0;\n  background: #fff7f7;\n  color: #b42318;\n}\n\n.admin-agent-actions .danger-action:hover:not(:disabled) {\n  border-color: #e6aaaa;\n  background: #fff0f0;\n}\n\n.admin-agent-actions .danger-action:disabled {\n  cursor: wait;\n  opacity: 0.58;\n}\n",
          )

          delete_route = r'''
adminConfigApi.delete('/api/admin/agents/:id', async (c) => {
  if (!(await adminAuthorized(c))) return unauthorized(c);
  const id = c.req.param('id');
  if (id === 'admin') return c.json({ error: 'NOT_FOUND' }, 404);

  const current = await c.env.DB.prepare(
    `SELECT id, name
     FROM agents
     WHERE id = ?1 AND site_id = 'default'`,
  )
    .bind(id)
    .first<{ id: string; name: string }>();
  if (!current) return c.json({ error: 'NOT_FOUND' }, 404);

  try {
    // Disable first so routing cannot assign new conversations while the
    // destructive cleanup is being prepared.
    await c.env.DB.prepare(
      `UPDATE agents
       SET is_enabled = 0,
           status = 'offline',
           last_seen_at = NULL,
           updated_at = CURRENT_TIMESTAMP
       WHERE id = ?1 AND site_id = 'default'`,
    )
      .bind(id)
      .run();

    const conversationsToReassign = await assignedActiveConversationIds(
      c.env.DB,
      id,
    );

    await c.env.DB.batch([
      c.env.DB.prepare('DELETE FROM agent_sessions WHERE agent_id = ?1').bind(id),
      c.env.DB.prepare(
        `UPDATE conversations
         SET assigned_agent = NULL,
             assigned_at = NULL,
             assigned_business_date = NULL,
             status = 'open',
             updated_at = CURRENT_TIMESTAMP
         WHERE assigned_agent = ?1
           AND status IN ('open', 'pending')
           AND expires_at > CURRENT_TIMESTAMP`,
      ).bind(id),
      c.env.DB.prepare(
        `UPDATE conversations
         SET cta_affinity_agent_id = NULL,
             cta_affinity_expires_at = NULL,
             updated_at = CURRENT_TIMESTAMP
         WHERE cta_affinity_agent_id = ?1`,
      ).bind(id),
      c.env.DB.prepare(
        `DELETE FROM agents
         WHERE id = ?1 AND site_id = 'default'`,
      ).bind(id),
    ]);

    if (conversationsToReassign.length) {
      await disconnectAgentRealtime(c.env, id, conversationsToReassign);
      for (const conversationId of conversationsToReassign) {
        await assignConversationAgent(c.env.DB, conversationId);
        await broadcastClientConversationEvent(
          c.env,
          conversationId,
          'conversation.assigned',
        );
      }
    }

    return c.json({
      ok: true,
      reassignedConversationCount: conversationsToReassign.length,
    });
  } catch (error) {
    console.error('agent.delete.failed', { agentId: id, error });
    return c.json({ error: 'AGENT_DELETE_FAILED' }, 500);
  }
});

'''
          replace_once(
              'src/worker/admin-config-api.ts',
              "adminConfigApi.get('/api/admin/products', async (c) => {\n",
              delete_route + "adminConfigApi.get('/api/admin/products', async (c) => {\n",
          )

          test_path = Path('test/customer-service-full-flow.test.mjs')
          test_text = test_path.read_text()
          marker = "admin permanently deletes an agent while preserving historical records"
          if marker not in test_text:
              test_text += r'''

test('admin permanently deletes an agent while preserving historical records', async () => {
  const database = new DatabaseSync(':memory:');
  applyMigrations(database);
  const adminPassword = 'admin-password';
  const rooms = fakeRooms();
  const env = {
    DB: d1(database),
    CONVERSATION_ROOMS: rooms.namespace,
    ADMIN_PASSWORD: adminPassword,
  };

  const createResponse = await adminConfigApi.request(
    '/api/admin/agents',
    {
      method: 'POST',
      headers: {
        cookie: adminCookie(adminPassword),
        'content-type': 'application/json',
      },
      body: JSON.stringify({
        name: 'Delete Me',
        username: 'delete-me',
        password: 'pass',
        routingScope: { type: 'none' },
        maxActiveConversations: 0,
        dailyConversationLimit: 0,
        trafficQuotaEnabled: true,
        trafficQuotaTopUp: 10,
        trafficQuotaRequestId: 'delete-agent-quota-001',
        isEnabled: true,
      }),
    },
    env,
  );
  assert.equal(createResponse.status, 201);
  const created = await createResponse.json();
  const agentId = created.id;

  database
    .prepare(
      `INSERT INTO agent_sessions (id, agent_id, token_hash, expires_at)
       VALUES (?, ?, ?, datetime('now', '+1 hour'))`,
    )
    .run('delete-agent-session', agentId, 'delete-agent-session-token');
  database
    .prepare(
      `INSERT INTO agent_routing_scopes (
         site_id, agent_id, scope_type, section_id, category_id, product_id
       ) VALUES ('default', ?, 'section', 'west', '', '')`,
    )
    .run(agentId);
  database
    .prepare(
      `INSERT INTO agent_daily_stats (
         site_id, agent_id, business_date, conversation_count
       ) VALUES ('default', ?, '2026-08-26', 3)`,
    )
    .run(agentId);
  database.exec(`
    INSERT INTO visitors (id, site_id, token_hash)
    VALUES
      ('delete-active-visitor', 'default', 'delete-active-token'),
      ('delete-history-visitor', 'default', 'delete-history-token');
  `);
  database
    .prepare(
      `INSERT INTO conversations (
         id, site_id, visitor_id, status, assigned_agent, assigned_at,
         assigned_business_date, expires_at, cta_affinity_agent_id,
         cta_affinity_expires_at
       ) VALUES (
         'delete-active-conversation', 'default', 'delete-active-visitor',
         'pending', ?, CURRENT_TIMESTAMP, '2026-08-26',
         datetime('now', '+1 hour'), ?, datetime('now', '+2 hours')
       )`,
    )
    .run(agentId, agentId);
  database
    .prepare(
      `INSERT INTO conversations (
         id, site_id, visitor_id, status, assigned_agent, assigned_at,
         assigned_business_date, expires_at, cta_affinity_agent_id,
         cta_affinity_expires_at
       ) VALUES (
         'delete-history-conversation', 'default', 'delete-history-visitor',
         'closed', ?, CURRENT_TIMESTAMP, '2026-08-26',
         datetime('now', '+1 hour'), ?, datetime('now', '+2 hours')
       )`,
    )
    .run(agentId, agentId);
  database
    .prepare(
      `INSERT INTO messages (
         id, conversation_id, sender_type, sender_id, body
       ) VALUES (
         'delete-history-message', 'delete-history-conversation',
         'agent', ?, 'historical reply'
       )`,
    )
    .run(agentId);

  const deleteResponse = await adminConfigApi.request(
    `/api/admin/agents/${encodeURIComponent(agentId)}`,
    {
      method: 'DELETE',
      headers: { cookie: adminCookie(adminPassword) },
    },
    env,
  );
  const deleted = await deleteResponse.json();
  assert.equal(deleteResponse.status, 200);
  assert.equal(deleted.reassignedConversationCount, 1);
  assert.equal(
    database.prepare('SELECT COUNT(*) AS count FROM agents WHERE id = ?').get(agentId).count,
    0,
  );
  assert.equal(
    database.prepare('SELECT COUNT(*) AS count FROM agent_sessions WHERE agent_id = ?').get(agentId).count,
    0,
  );
  assert.equal(
    database.prepare('SELECT COUNT(*) AS count FROM agent_routing_scopes WHERE agent_id = ?').get(agentId).count,
    0,
  );
  assert.equal(
    database.prepare('SELECT COUNT(*) AS count FROM agent_quota_adjustments WHERE agent_id = ?').get(agentId).count,
    0,
  );

  const active = database
    .prepare(
      `SELECT status, assigned_agent, cta_affinity_agent_id
       FROM conversations WHERE id = 'delete-active-conversation'`,
    )
    .get();
  assert.equal(active.status, 'open');
  assert.equal(active.assigned_agent, null);
  assert.equal(active.cta_affinity_agent_id, null);

  const historical = database
    .prepare(
      `SELECT status, assigned_agent, cta_affinity_agent_id
       FROM conversations WHERE id = 'delete-history-conversation'`,
    )
    .get();
  assert.equal(historical.status, 'closed');
  assert.equal(historical.assigned_agent, agentId);
  assert.equal(historical.cta_affinity_agent_id, null);
  assert.equal(
    database
      .prepare("SELECT sender_id FROM messages WHERE id = 'delete-history-message'")
      .get().sender_id,
    agentId,
  );
  assert.equal(
    database
      .prepare(
        `SELECT conversation_count
         FROM agent_daily_stats
         WHERE site_id = 'default' AND agent_id = ? AND business_date = '2026-08-26'`,
      )
      .get(agentId).conversation_count,
    3,
  );
});
'''
              test_path.write_text(test_text)

          contract = Path('test/agent-delete-ui-contract.test.mjs')
          contract.write_text(r'''import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import test from 'node:test';

const portal = readFileSync(new URL('../src/dashboard/AdminPortal.tsx', import.meta.url), 'utf8');
const api = readFileSync(new URL('../src/dashboard/api.ts', import.meta.url), 'utf8');

test('admin agent list exposes confirmed permanent deletion', () => {
  assert.match(portal, /deleteAgent/);
  assert.match(portal, /danger-action/);
  assert.match(portal, /确定永久删除客服/);
  assert.match(api, /method: 'DELETE'/);
});
''')

          Path('.github/workflows/agent-delete-repair.yml').unlink()
          PY

          pnpm exec prettier --write \
            src/dashboard/api.ts \
            src/dashboard/AdminPortal.tsx \
            src/dashboard/admin-agents.css \
            src/worker/admin-config-api.ts \
            test/customer-service-full-flow.test.mjs \
            test/agent-delete-ui-contract.test.mjs

      - name: Validate repair
        run: |
          pnpm format
          pnpm lint
          pnpm typecheck
          pnpm test
          pnpm build
          pnpm cf:check

      - name: Commit repair
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Ali Lee"
          git config user.email "fcqz121314@gmail.com"
          git add -A
          git commit -m "fix(admin): restore agent deletion"
          git push origin HEAD:fix/restore-agent-delete
