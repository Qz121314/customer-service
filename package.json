{
  "name": "customer-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "dev:ui": "vite --host 0.0.0.0 --port 5173",
    "prepare": "node scripts/setup-git-hooks.mjs",
    "build": "pnpm build:ui && pnpm typecheck:worker",
    "build:ui": "vite build",
    "guardrails": "node scripts/check-repository-guardrails.mjs",
    "cf:types": "wrangler types src/worker/worker-configuration.d.ts",
    "cf:check": "pnpm build:ui && wrangler deploy --dry-run",
    "cf:provision": "node scripts/provision-cloudflare.mjs",
    "deploy:cloudflare": "pnpm cf:provision && pnpm db:migrate:remote && wrangler deploy --keep-vars --x-provision --x-auto-create",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "preflight": "pnpm guardrails && pnpm format && pnpm lint && pnpm typecheck",
    "verify": "pnpm preflight && pnpm db:migrate:local && pnpm test && pnpm build && pnpm cf:check",
    "smoke:production": "node scripts/smoke-production.mjs",
    "typecheck": "pnpm typecheck:ui && pnpm typecheck:worker",
    "typecheck:ui": "tsc -p tsconfig.app.json --noEmit",
    "typecheck:worker": "pnpm cf:types && tsc -p tsconfig.worker.json --noEmit",
    "test": "node --experimental-strip-types --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@radix-ui/react-slot": "1.3.0",
    "class-variance-authority": "0.7.1",
    "clsx": "2.1.1",
    "hono": "^4.0.0",
    "lucide-react": "1.21.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "3.6.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@tailwindcss/vite": "4.3.3",
    "@vitejs/plugin-react": "^4.3.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.0",
    "globals": "^16.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0",
    "tailwindcss": "4.3.3",
    "vite": "^6.0.0",
    "wrangler": "^4.123.0"
  }
}
